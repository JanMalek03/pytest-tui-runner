category:
  - name: "UI"
    description: "Testy zaměřené na uživatelské rozhraní"
    subcategory:
      - name: "Login"
        tests:
          - name: "Login Test"
            marker: "login"
            widget: "checkbox"
            parameters: {}
          - name: "Logout Test"
            marker: "logout"
            widget: "text_input"
            parameters:
              placeholder: "Zadej text"
    subcategory:
      - name: "Navigace"
        tests:
          - name: "Menu Test"
            marker: "menu"
            widget: "dropdown"
            parameters:
              options: ["Home", "Profile", "Settings"]
category:
  - name: ""        # Empty name for the category
    description: "Testy API endpointů"
    subcategory:
      - name: ""    # Empty name for the subcategory
        tests:
          - name: "GET /users"
            marker: "users"
            widget: "dropdown"
            parameters:
              options: ["admin", "user", "guest"]
