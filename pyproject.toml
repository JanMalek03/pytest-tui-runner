[project]
name = "pytest-gui"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "loguru>=0.7.3",
    "pytest>=8.3.5",
    "pyyaml>=6.0.2",
    "textual>=3.0.0",
]
